# Core dependencies for the GenPLIP pipeline (stage0a - stage9a).
# Generated to match pyproject.toml — install with either:
#   uv sync                       (recommended, see .python-version / pyproject.toml)
#   pip install -r requirements.txt
torch>=2.0
transformers>=4.35
rdkit>=2023.9.1
selfies>=2.1
pandas>=2.0
numpy>=1.26
matplotlib>=3.7
tqdm>=4.65
peft>=0.11
torchao>=0.16.0
chembl-webresource-client>=0.10

# Optional: only needed for stage1c_upload_pdb_plip_to_gdrive.py
# google-api-python-client>=2.0
# google-auth-oauthlib>=1.0
# google-auth-httplib2>=0.1
